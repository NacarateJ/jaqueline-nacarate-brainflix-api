### Add .env file with your preferred port number
    <!-- PORT=YOUR PORT NUMBETR -->

### run `npm i` in the root folder to instal node_modules

### To start the sever, run `npm run server` in the terminal
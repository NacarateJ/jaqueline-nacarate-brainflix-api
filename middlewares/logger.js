// const moment = require("moment");

//middleware to log all the requests made to the server (what kind of API was clalled, what time) 
// const logger = (req, _res, next) => {
//   console.log(
//     `${req.protocol}://${req.get("host")}${
//       req.originalUrl
//     } : ${moment().format()}`
//   );

  // do NOT forget to pass the api call to the next middleware
//   next();
// };

// module.exports = logger;
